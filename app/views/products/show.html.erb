<div class = 'container'>

  <%=@product.name %>
  <%=@product.price %>
  <%@product.categories.each do |category|%>
  <ul>
    <li><%=category.name%></li>
  </ul>
  <% end %>
  <%= link_to 'Edit', edit_product_path(@product) %> | <%= link_to 'All Products', products_path %>
  <div>
